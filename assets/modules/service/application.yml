spring:
  application:
    name: {{.ArtifactID}}

server:
  port: {{.Port}}

lark:
  project:
    type: service

---
spring:
  config:
    activate:
      on-profile: playground

lark:
  registry:
    address: register-dev.lark-cloud.com:8848